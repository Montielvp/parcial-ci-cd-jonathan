{
  "name": "parcial-ci-cd",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "jest --coverage",
    "build": "node -e \"console.log('build step ejecutado')\"",
    "check-coverage": "nyc check-coverage --lines 80 --functions 80 --branches 80 --statements 80",
    "ci": "npm run lint && npm run build && npm run test && npm run check-coverage"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "nyc": "^15.0.0"
  }
}
